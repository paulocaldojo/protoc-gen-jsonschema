syntax = "proto3";
package samples;

message Money {
    enum Currency {
        NOT_SPECIFIED = 0;
        USD = 1;
        GBP = 2;
        EUR = 3;
    }

    int64 amount = 1;
    Currency currency = 2;
}
